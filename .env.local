# [임시] 로컬 프론트 → GCP VM(검출 백엔드) 연결용. 검증 끝나면 삭제.
# 모든 요청을 dev 서버(5173) 자신으로 → vite 프록시(/api,/stream,/ws) → GCP VM 으로 전달.
# 같은 origin(http localhost)이라 CORS/mixed-content 없음.
VITE_API_BASE_URL=http://localhost:5173
VITE_WS_URL=ws://localhost:5173/api/v1/ws
VITE_PROXY_TARGET=http://34.64.124.77:8000
